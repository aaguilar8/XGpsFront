import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#0f80aa',
            contrastText: '#fff',
        },

        secondary: {
            main: '#f44336',
            contrastText: '#fff',
        },
    },
})

export default theme;